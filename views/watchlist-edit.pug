extends layout

include utils.pug

block content
    +validationErrorSummary(errors)
    form(action=`/watchlists/${watchlistId}/edit` method="post")
        input(type="text", name="name")
        button(type="submit") done
