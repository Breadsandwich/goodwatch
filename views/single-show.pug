extends layout.pug

include utils.pug

block content
    table
            thead
                tr
                    th Name
                    th Description
                    th Overall Rating
                    th Watch Status
                    th Genre
            tbody
                +showTable(show)
                a(href='/shows/all') Back to Shows
                a(href=`/shows/${show.id}/reviews`) write a review
    //- div
    //-     table
    //-         thead
    //-             tr
    //-                 th review
    //-                 th rating
    //-         tbody
    //-             +reviewTable(review)
